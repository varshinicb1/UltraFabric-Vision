# UltraFabric-Vision — API / inference server dependencies (headless, no GUI).
# Use this for the production REST server and Docker image. The desktop PyQt app
# uses requirements.txt instead.
#
# NOTE: torch/torchvision are intentionally NOT pinned here — install the build
# that matches your CUDA runtime FIRST (see DEPLOYMENT.md), e.g.:
#   pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cu121
# The GPU Dockerfile starts from an image that already ships CUDA-enabled torch.

torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
numpy>=1.24.0,<2.0
opencv-python-headless>=4.7.0
scikit-learn>=1.2.0
joblib>=1.3.0
pillow>=9.5.0
pyyaml>=6.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# QC PDF batch reports
reportlab>=4.0.0

# Optional GPU telemetry (safe to omit; code degrades gracefully)
py3nvml>=0.2.7
